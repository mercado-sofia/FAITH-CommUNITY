'use client';

// Temporary placeholder to prevent import errors
// Highlights component is currently disabled
export default function Highlights({ onClose, organizationId = null }) {
  return null;
}
